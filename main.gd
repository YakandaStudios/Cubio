extends Node

enum state{
	STATE_STANDING,
	STATE_JUMPING,
	STATE_DOUBLEJUMP,
	STATE_TURNLEFT,
	STATE_TURNRIGHT,
	STATE_MOVEFRONT,
	STATE_MOVEBACK,
}
#state.STATE_STANDING


func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass
